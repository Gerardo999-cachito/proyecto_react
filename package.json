{
  "homepage": "https://github.com/Gerardo999-cachito/proyectogerardo.git",

  "dependencies": {
    "gh-pages": "^6.2.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build"
  }
}
